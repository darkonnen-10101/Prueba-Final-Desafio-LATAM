<div class="container">
  <h1>Projects</h1>

  <div class="row">
    <% @projects.reverse.each do |project| %>
      <div class="col-md-4 col-lg-4">
        <div class="card shadow border-1">
          <% if user_signed_in? %>
            <%= link_to (image_tag project.project_photo.url, size: "400x300", class: "card-img-top"), all_project_path(project), :remote => true%>
          <% else %>
          <%= link_to (image_tag project.project_photo.url, size: "400x300", class: "card-img-top"), all_project_path(project), :remote => true%>
        <% end %>
        </div>
        <div class="w-100" style="margin-top:20px;">
        </div>

      </div>
      <% end %>

      <!-- <a href='https://www.flow.cl/btn.php?token=0betiwg' target='_blank'>
        <img src='https://www.flow.cl/img/botones/btn-donar-celeste.png'>
      </a> -->

      <div class="">
        <%= link_to "Sign in with Github", user_github_omniauth_authorize_path %>
      </div>



  </div>
</div>
