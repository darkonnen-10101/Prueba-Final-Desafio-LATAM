<div class="container">

  <h1>Projects</h1>

  <table class="table table-striped table-hover ">
    <thead>
      <tr>
        <th>Name</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @categories.reverse.each do |category| %>

        <tr>
          <td><%= link_to "#{category.name}", category %></td>
          <td>
            <%= link_to 'Editar', root_path(project), class: 'btn btn-sm btn-success' %>
          </td>
          <td>
            <%= link_to 'Eliminar', category, method: :delete, data: { confirm: 'Estás seguro que deseas eliminar la category?' }, class: 'btn btn-sm btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Project', new_category_path, class: 'btn btn-primary' %>
</div>
